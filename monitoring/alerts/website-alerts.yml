groups:
  - name: website-alerts

    rules:
      - alert: WebsiteDown

        expr: probe_success == 0

        for: 1m

        labels:
          severity: critical

        annotations:
          summary: Website is down
          description: Blackbox probe failed for more than 1 minute.
